spring:
  datasource:
    url: jdbc:mysql://localhost:3306/apidatabase?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: off

server:
  error:
    include-stacktrace: never

